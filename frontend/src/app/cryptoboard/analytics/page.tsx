import TransactionList from "./TransactionList";

const Page = () => {
  return (
    <div className="p-6 bg-[var(--black0)] w-full h-full overflow-y-auto">
      <TransactionList />
    </div>
  );
};

export default Page;
